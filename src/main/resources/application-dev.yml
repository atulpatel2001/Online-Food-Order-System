spring:
  datasource:
    url: jdbc:postgresql://localhost:3307/online-food-order
    username: postgres
    password: root123


  jpa:
    show-sql: true
    hibernate:
      ddl-auto: update
    properties:
      hibernate:
        format_sql: true
        dialect: org.hibernate.dialect.PostgreSQLDialect